<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HikeManagementApp" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HikeManagementApp" parent="Base.Theme.HikeManagementApp" />
    
    <style name="Theme.HikeManagementApp.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    
    <style name="Theme.HikeManagementApp.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>